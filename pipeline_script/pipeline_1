pipeline {
    agent any
    stages("Complile") {
        steps {
            echo "Complile successully!!";
        }

        stage("JUnit") {
            steps {
                echo "JUnit passed successully!!"";
            }
        }

        stage("Quality-Gate") {
            steps {
                echo "Quality-Gate passed successully!!";
                /*sh exit ("1");*/
            }
        }

        stage("Deploy") {
            steps {
                echo "Pass"!;
            }
        }
    }

    post {
        always {
            echo "This will always run"
        }
        succuess {
            echo "This will run only if successfull"
        }
        failure {
            echo "This will run only if failed"
        }
        unstable {
            echo "This will run only if the run was marked as unstable"
        }
        changed {
            echo "This will run only if the state of the Pipeline has changed"
            echo "For example, if the Pipeline was previously failing but is now succsessful"
        }
    }
}